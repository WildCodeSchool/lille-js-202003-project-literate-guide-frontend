import { makeStyles } from '@material-ui/core/styles';

const listButtonStyles = makeStyles({
  root: {
    background: '#FFFFFF',
    zIndex: 2,
    position: 'absolute',
    bottom: '90px',
    right: '27px',
  },
});

export default listButtonStyles;
